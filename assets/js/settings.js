import {github} from './github.js';import {getTheme,applyTheme} from './theme.js';import {esc} from './utils.js';
export function renderSettings(db,state){const c=github.getConfig();return `<section class="page-head"><div><span class="eyebrow">05 / SETTINGS</span><h2>設定</h2></div><p>管理 GitHub 資料來源、同步狀態與顯示偏好。</p></section><section class="settings-grid"><div class="content-frame"><div class="section-header"><h3>GitHub</h3></div><div class="setting-row"><span>登入狀態</span><strong>尚未連線</strong></div><div class="setting-row"><span>帳號</span><strong>未登入</strong></div><div class="setting-row"><span>Repository</span><strong>${esc(c.owner+'/'+c.repository)}</strong></div><div class="setting-row"><span>Branch</span><strong>${esc(c.branch)}</strong></div></div><div class="content-frame"><div class="section-header"><h3>資料來源</h3></div><div class="setting-row"><span>Data path</span><strong>${esc(c.dataPath)}</strong></div><div class="setting-row"><span>Image path</span><strong>${esc(c.imagePath)}</strong></div><div class="setting-row"><span>資料庫版本</span><strong>Schema ${db.schemaVersion}</strong></div><div class="setting-row"><span>最後更新</span><strong>${esc(db.meta.updatedAt||'尚未更新')}</strong></div></div><div class="content-frame"><div class="section-header"><h3>同步</h3></div><div class="notice">${esc(state.sync.message||'尚未同步')}</div><div class="actions"><button class="button primary" id="manual-sync">手動同步</button></div></div><div class="content-frame"><div class="section-header"><h3>顯示設定</h3></div><div class="form-field"><label>Theme</label><select class="select" id="theme-select"><option value="system">跟隨系統</option><option value="light">亮色</option><option value="dark">暗色</option></select></div></div></section>`}
export function bindSettings(){const s=document.getElementById('theme-select');if(s){s.value=getTheme();s.addEventListener('change',()=>applyTheme(s.value))}}
